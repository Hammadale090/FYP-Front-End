import ActiveListings from '@/features/ActiveListings/page'
import React from 'react'

type Props = {}

const page = (props: Props) => {
    return (
        <div>
            <ActiveListings />
        </div>
    )
}

export default page