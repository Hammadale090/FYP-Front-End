import Property from '@/features/Property'
import React from 'react'

type Props = {}

const page = (props: Props) => {
    return (
        <div>
            <Property />
        </div>
    )
}

export default page