import CreateProperty from "@/features/Property/CreateProperty";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <CreateProperty />
    </div>
  );
};

export default page;
